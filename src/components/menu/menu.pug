
.menu
    ul.menu__list

        each item, index in [...Array(25).keys()]
            li(class=`menu__item${i === 0 ? 'menu__item--active' : ''}`)
                    .menu__circle.skeleton
                    .menu__line.skeleton(class=`menu__line--${item  + 20}`)